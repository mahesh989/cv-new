import 'package:flutter/material.dart';
import 'cv_page.dart';

class CvPageExtended extends StatelessWidget {
  const CvPageExtended({super.key});

  @override
  Widget build(BuildContext context) {
    return const CvPage(); // basic wrapper if needed for legacy use
  }
}
